packages:
  - "packages/**/*"
allowBuilds:
  esbuild: true
# drizzle-kit migrate over the Data API fails with newer client versions
# (https://github.com/drizzle-team/drizzle-orm/issues/5050).
overrides:
  "@aws-sdk/client-rds-data": 3.873.0
